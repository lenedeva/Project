package com.example;

public class TaskHard {
    public static void main(String[] args) {
        String str = "Стэндофф";
        String subString = "офф";
        int index = str.indexOf(subString);
        System.out.println(index);
    }

    // Задача (сложная): валидация email и пароля
    // Написать два метода (можно и в один метод уместить):
    // 1) Первый метод проверяет валидность email. 
    //    Email должен содержать @. (подсказка в методе main этого класса) 
    // 2) Второй метод отвечает за 
    //     валидность пароля (минимум 8 символов)

    public static boolean isValidData(String email, String password) {
        
        
        return true;
    }

}
